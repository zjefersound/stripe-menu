import React from 'react';

// import { Container } from './styles';

interface Props {
  name: string;
  content: any;
}
export const DropdownOption: React.FC<Props> = ({ name, content }) => {
  return (
    <button>

    </button>
  );
}
